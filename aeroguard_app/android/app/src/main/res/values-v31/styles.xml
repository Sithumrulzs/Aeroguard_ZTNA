<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31) shows its own system splash using the app's
         launcher icon by default, before Flutter even draws — that's the
         logo flash we don't want ahead of the intro video. Override it with
         a solid, icon-free splash instead. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
